# 产品路线

更新时间：2026-07-14

## 已完成

- SPEC-001-A：质量数据 Data Scope 授权与验证已关闭。
- SPEC-001-B：报告生命周期（草稿、审核中、已发布）、发布快照、审计与运行时权限验收已关闭。

## 后续候选（未排期）

- 来源检测记录选择器：按 `record_no`、零件、日期等业务信息选择并写入 `inspection_record.id`。
- 分析结果实体化：建立权威 `analysis_result` 或等价实体，替代仅校验非空的 `analysis_identifiers`。
- 共享查询契约：统一 Dashboard、检测台账与导出的统计及筛选逻辑。

后续能力必须通过独立 SPEC 定义范围、数据影响和验收标准，不自动继承为本阶段实现。
